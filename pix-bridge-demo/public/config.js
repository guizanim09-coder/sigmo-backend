window.APP_CONFIG = window.APP_CONFIG || {
  // URL publica do backend no Railway.
  // Exemplo: "https://sigmo-backend.up.railway.app"
  // Em branco, o frontend usa o mesmo dominio atual, o que ajuda em testes locais.
  API_BASE_URL: ""
};
